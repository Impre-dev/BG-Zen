# Pools d'images perso — jamais commités (règle data vs code)
backgrounds/*
!backgrounds/default.png
loadings/*
!loadings/.gitkeep

# Boot splash : image perso + cache citation — data runtime
boot/*

# Log debug runtime — data, jamais commité
bgzen-debug.log
